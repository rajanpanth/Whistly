[workspace]
members = [
    "programs/instinctfi",
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
# Optimize for size — keeps devnet deploy/upgrade buffer rent low.
opt-level = "z"

[profile.dev]
overflow-checks = true
